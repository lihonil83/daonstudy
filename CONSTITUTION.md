# CONSTITUTION.md

## 목적

이 문서는 daonstudy 저장소의 헌법이다.
프로젝트의 목표, 절대 깨지면 안 되는 원칙, 기술적 비가역 결정, 문서 체계의 최상위 기준을 정의한다.

세부 기능 스펙이나 실행 순서는 다른 문서가 담당하지만, 그 문서들은 모두 이 헌법의 범위 안에서만 변경될 수 있다.

---

## 1. 프로젝트 정체성

1. 이 프로젝트는 초등학교 2학년 아들 1명을 위한 개인용 영어/수학 학습 웹 앱이다.
2. 이 프로젝트의 핵심 목적은 "공부 앱"보다 "게임처럼 느껴지는 학습 경험"을 만드는 것이다.
3. 사용자는 아이 1명이며, 비교/경쟁/랭킹 기능은 핵심 목적에 포함되지 않는다.
4. 학습 경험은 자기 주도적이어야 하며, 죄책감이나 압박을 유도하면 안 된다.

---

## 2. 교육 철학

1. 놀이는 학습보다 앞선다.
2. 오답은 벌이 아니라 다시 배우는 기회다.
3. 아이의 속도를 존중한다.
4. 작은 성취도 크게 축하한다.
5. 설명 없이도 사용할 수 있을 만큼 단순해야 한다.

교육 철학의 상세 표현과 UX 판단 기준은 `docs/design-docs/core-beliefs.md`가 담당한다.

---

## 3. 기술 헌법

아래 항목은 현재 저장소의 기본 기술 헌법이다.

- 프레임워크: `React 18`
- 빌드 도구: `Vite`
- 언어: `JavaScript`
- 스타일링: `CSS Modules`
- 라우팅: `React Router v6 + HashRouter`
- 저장: `localStorage`
- 실행 모델: `Vite build` 산출물을 정적 파일로 실행

다음 항목은 기본 금지 원칙이다.

- 서버 코드 추가 금지
- 외부 API 의존 금지
- TypeScript 도입 금지
- 전역 상태 라이브러리 도입 금지
- 불필요한 외부 패키지 추가 금지

---

## 4. 실행 환경 헌법

1. 개발 환경은 macOS 기준으로 진행한다.
2. 실행 확인은 Windows 브라우저의 정적 파일 실행을 기준으로 한다.
3. 라우팅은 반드시 `HashRouter` 기반이어야 한다.
4. `vite.config.js`의 `base: './'` 전제를 깨면 안 된다.
5. 오프라인 실행 가능성이 항상 우선한다.

---

## 5. 데이터 헌법

1. 학습 데이터는 `src/data/` 아래 JSON 파일로 관리한다.
2. 문제 데이터는 정적 import 기반으로 앱에 포함된다.
3. 브라우저 저장 데이터는 `eduapp_` 접두사를 사용한다.
4. localStorage의 상세 구조는 각 product spec 문서가 정본이다.
5. 힌트 구조는 `hints` 배열이며, 2단계 힌트 후 정답 공개 흐름을 유지한다.

---

## 6. MVP 헌법

현재 MVP의 고정 범위는 아래와 같다.

- 수학: 구구단
- 영어: 알파벳

MVP의 전체 Phase 정의는 `PLANS.md`가 정본이고, 현재 실행 세부 항목은 `docs/exec-plans/active/mvp-plan.md`가 정본이다.
둘은 서로 충돌해서는 안 된다.

---

## 7. 문서 체계 헌법

1. 루트 문서는 방향과 요약을 담당한다.
2. `docs/` 아래 문서는 상세 스펙과 실행 계획을 담당한다.
3. 같은 주제를 여러 문서가 다룰 때는 정본 문서가 반드시 하나 있어야 한다.
4. 요약 문서는 정본 문서와 다른 의미를 가져서는 안 된다.
5. 문서 간 충돌은 구현보다 먼저 해결해야 한다.

문서 정본 체계와 변경/검증 절차의 상세 운영 규칙은 `REPOSITORY-HARNESS.md`가 담당한다.

---

## 8. 변경 통치 원칙

다음 변경은 헌법 수준 변경으로 본다.

- MVP 범위 변경
- 기술 스택 변경
- 라우팅 방식 변경
- 실행 모델 변경
- 오프라인 우선 원칙 변경
- 데이터 저장 방식 변경
- 교육 철학의 핵심 원칙 변경

헌법 수준 변경은 관련 문서를 동시에 갱신해야 하며, 하위 문서만 먼저 바꾸면 안 된다.

---

## 9. 우선순위 규칙

문서가 충돌할 때의 우선순위는 아래와 같다.

1. 헌법 수준 원칙은 `CONSTITUTION.md`가 우선한다.
2. 문서 운영과 정합성 규칙은 `REPOSITORY-HARNESS.md`가 우선한다.
3. 기능 상세 계약은 `docs/product-specs/` 아래 문서가 우선한다.
4. 실행 계획은 `PLANS.md`와 `docs/exec-plans/active/mvp-plan.md`를 함께 보되, 충돌 시 둘 다 수정한다.
5. 루트 요약 문서는 하위 정본 문서를 대체할 수 없다.

---

## 10. 완료 상태의 정의

이 저장소가 헌법을 지키고 있다는 뜻은 아래와 같다.

- 문서만 읽어도 프로젝트의 목적과 경계가 흔들리지 않는다.
- 구현이 바뀌어도 오프라인/로컬/아이 친화적 원칙은 유지된다.
- 상세 스펙이 바뀌어도 정체성, 기술 기반, 문서 체계는 쉽게 흔들리지 않는다.
